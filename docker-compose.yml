services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_DB: circomap
      POSTGRES_USER: circomap
      POSTGRES_PASSWORD: circomap
    ports: ["5432:5432"]
    volumes: [dbdata:/var/lib/postgresql/data]
volumes:
  dbdata: {}
